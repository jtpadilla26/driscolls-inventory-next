# -------- Netlify config (Next.js SSR) --------
[build]
  command = "npm ci && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NPM_CONFIG_LOGLEVEL = "verbose"
  CI = "true"

[[plugins]]
  package = "@netlify/plugin-nextjs"

